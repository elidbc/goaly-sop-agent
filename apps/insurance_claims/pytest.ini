[pytest]
# Add this folder and the tests folder to the import path. Then "import sop_agent" works in the tests.
pythonpath = . tests
markers =
    llm: tests that call the real AI model (slow, cost money)
# By default, run only the fast tests. Run the LLM tests with:  pytest -m llm -s
addopts = -m "not llm"
