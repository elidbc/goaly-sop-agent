# The Python packages for this app. Install them with:  pip install -r requirements.txt
anthropic        # SDK for the Anthropic adapter.
openai           # SDK for the OpenAI-compatible adapter (OpenAI, OpenRouter, ...).
pydantic>=2      # Data classes that check their fields. Also used for structured LLM output.
gradio>=5        # The chat UI in the web browser.
python-dotenv    # Reads settings from a local .env file.
pytest           # Runs the tests.
